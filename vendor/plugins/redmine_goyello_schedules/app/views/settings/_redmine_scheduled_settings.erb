<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>
<%
status_id = @settings['quickIssueStatus'].to_i;
if(status_id == -1)
  status_id = SchedulesController.fetch_default_status_id
end
%>
<p>
  <label><%= l(:field_is_default) %> </label>
  <%= select_tag 'settings[quickIssueStatus]',
    options_for_select( IssueStatus.all.collect {|is| [is.name, is.id]}, @settings['quickIssueStatus'].to_i) %>
</p>